# from zai import ZhipuAiClient
#
# client = ZhipuAiClient(api_key="28e4d3fb29794859af3a2d1e156392d6.WowEvTmCmTzt02PN")
#
# response = client.web_search.web_search(
#    search_engine="search_pro",
#    search_query="搜索2025年4月的财经新闻",
#    count=15,  # 返回结果的条数，范围1-50，默认10
#    search_domain_filter="",  # 只访问指定域名的内容
#    search_recency_filter="noLimit",  # 搜索指定日期范围内的内容
#    content_size="high"  # 控制网页摘要的字数，默认medium
# )
# print(response)

input_messages_key = 'content'

print(f'{{{input_messages_key}}}')

print(f'{{input_messages_key}}')

